import { supabase } from '@/lib/supabase';

/**
 * seedDatabase is intentionally left empty to avoid inserting test data.
 * It can be extended in the future to populate initial real data if needed.
 */
export async function seedDatabase(_userId: string): Promise<void> {
  // No se insertan datos de prueba.
  return;
}
